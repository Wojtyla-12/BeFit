@model IEnumerable<BeFitt.Models.Sesja>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Start_Czas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Koniec_Czas)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dzien_Start)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Dzien_Koniec)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Start_Czas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Koniec_Czas)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dzien_Start)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Dzien_Koniec)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
